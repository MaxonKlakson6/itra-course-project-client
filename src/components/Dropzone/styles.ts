import styled from '@emotion/styled';

export const Wrapper = styled.div`
  cursor: pointer;
`;

export const LoadImage = styled.img`
  width: 300px;
  height: 200px;
  display: block;
  object-fit: cover;
`;
